\documentclass[border=6pt]{standalone}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,fit,backgrounds,shapes.geometric}
\definecolor{navy}{HTML}{161E29}
\definecolor{slate}{HTML}{2E3D50}
\definecolor{slatefill}{HTML}{E9EDF2}
\definecolor{accent}{HTML}{B45309}
\definecolor{accentfill}{HTML}{FEF3C7}
\definecolor{edge}{HTML}{51647D}
\begin{document}
\begin{tikzpicture}[
  font=\sffamily\small,
  box/.style={rounded corners=3pt, draw=slate, line width=0.7pt, fill=slatefill, text=navy, align=center, inner sep=5pt, minimum height=10mm, minimum width=34mm},
  dark/.style={box, fill=navy, draw=navy, text=white, minimum width=58mm},
  acc/.style={box, draw=accent, line width=1.1pt, fill=accentfill, text=navy},
  lbl/.style={font=\sffamily\scriptsize, text=slate},
  arr/.style={-{Stealth[length=2.4mm]}, line width=0.8pt, draw=edge},
  biarr/.style={{Stealth[length=2.4mm]}-{Stealth[length=2.4mm]}, line width=0.9pt, draw=accent},
]
\node[box, minimum width=58mm] (spa) {\textbf{Browser $\cdot$ React SPA}\\{\scriptsize storefront $\cdot$ chat widget $\cdot$ dashboards}};
\node[dark, below=12mm of spa] (api) {\textbf{FastAPI $\cdot$ single origin}\\{\scriptsize serves SPA + /api/v1 $\cdot$ CSRF $\cdot$ rate limit}};
\node[box, below=14mm of api, xshift=-40mm] (svc) {Services\\{\scriptsize cart $\cdot$ orders $\cdot$ memory}};
\node[acc, below=14mm of api] (aisys) {AI subsystem\\{\scriptsize router $\cdot$ tools $\cdot$ RAG}};
\node[box, below=14mm of api, xshift=40mm] (ext) {External APIs\\{\scriptsize Claude $\cdot$ Stripe}};
\node[box, minimum width=80mm, below=12mm of aisys] (db) {\textbf{SQLAlchemy ORM} $\rightarrow$ SQLite (dev) \,/\, Postgres + pgvector (prod)};
\draw[biarr] (spa) -- node[lbl, right, pos=0.5] {REST + SSE} (api);
\draw[arr] (api) -- (svc);
\draw[arr] (api) -- (aisys);
\draw[arr] (api) -- (ext);
\draw[arr] (svc) -- (db);
\draw[arr] (aisys) -- (db);
\draw[arr] (aisys.east) to[bend right=20] (ext.south);
\end{tikzpicture}
\end{document}
